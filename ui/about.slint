import { AboutSlint } from "std-widgets.slint";
import { Button, VerticalBox, SpinBox, GroupBox, HorizontalBox, Switch } from "std-widgets.slint";

export component About inherits Window {
    always-on-top: true;
    
    // height: 175px;
    AboutSlint {
    }
    Button {
      text: "Close";
      clicked => { root.visible = false; }
  }
}